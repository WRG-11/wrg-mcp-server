services:
  - type: web
    name: wrg-mcp-server
    runtime: docker
    rootDir: apps/wrg_mcp_server
    autoDeploy: true
    envVars:
      - key: MCP_TRANSPORT
        value: streamable-http
      - key: MCP_HOST
        value: "0.0.0.0"
